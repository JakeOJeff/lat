nat coins = 250
nat incrementVal = 20
nat multiplier = 2

if (-K:isDown("space"))
    incrementCoin(3)
done

if (a == (2 or 3 or 5))
    
done

call incrementCoin(externalMultiplier)
    coins = coins + incrementVal * multiplier * externalMultiplier 
    return coins
done

print(coins)