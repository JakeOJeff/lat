

color = {0.5,0.5,0.5}

call love.load()
    thing_item = {
        "wow", 2, "glim glim"
    }
done

call love.update(dt)
    if -K:isDown("e")
        color = {1,1,1}

    done
done


call love.draw()
    -G:setColor(color)
    -G:print("hello")
done