nat coins = 250
nat incrementVal = 20
nat multiplier = 2

if (-K:isDown("space"))
    incrementCoin(3)
elif (-K:isDown("w"))
    incrementCoin(3)
done


call incrementCoin(externalMultiplier)
    coins = coins + incrementVal * multiplier * externalMultiplier 
    return coins
done

when (true)

done

switch score 
    to 10
        print("perfect")
    to 5
        print("ok")

done


print(coins)